# Skillwise Inventory Management (Single Server)

This project runs **frontend (React + Vite)** and **backend (Node + Express + SQLite)** on a single server.

## Commands

From the project root:

```bash
npm run install-all   # installs backend + frontend
npm start             # builds frontend and starts backend on http://localhost:5000
```
